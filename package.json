{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "exit 0",
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run build --stream --parallel",
    "lerna:version": "lerna version prerelease --preid beta && npm run lerna:postversion",
    "lerna:postversion": "git add CHANGELOG.md && git commit -m \"Update: changelog\" && git push --follow-tags",
    "prepublish": "lerna run prepublish",
    "lerna:publish": "lerna publish from-git --yes"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
