{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "exit 0",
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run build --stream --parallel",
    "lerna:version": "lerna version --conventional-commits --create-release github"
    //"lerna:postversion": "git add CHANGELOG.md && git commit -m \"Update: changelog\" && git push --follow-tags"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
